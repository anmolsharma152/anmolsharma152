# anmolsharma152 — setup

This is a **profile README** repo, not an application.

```bash
cd ~/Projects/anmolsharma152
# Edit README.md → commit → push main
# Contribution graph SVGs are referenced from the output branch / raw.githubusercontent.com
```

No runtime env required. Never put secrets in the profile README.
